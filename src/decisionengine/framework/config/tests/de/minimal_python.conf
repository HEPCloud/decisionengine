{
  'logger': {
    'log_file': '{}/de_log/decision_engine_log'.format(os.environ.get('HOME')),
    'max_file_size': 200 * 1000000,
    'max_backup_count': 6,
    'log_level': "DEBUG",
    'start_q_logger': "False",
  }
}
